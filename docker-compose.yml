version: "3.9"

services:
  cve-bot:
    build: .
    container_name: cve_alert_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data     # persists the SQLite DB and logs
    environment:
      - TZ=UTC
